# Misc

```bash
cd my_stuff/warm_transfer
lk agent dev agent_one.py
lk agent dev agent_two.py
```

Then call into agent one (twilio number +1-724-806-1667)

then I think wait 10 seconds and it will transfer you to the second agent
